🛠️ Compile all files:

bash
Copy
Edit
javac *.java
🌀 Start RMI registry (in same folder):

bash
Copy
Edit
start rmiregistry
🖥️ Run the server:

bash
Copy
Edit
java ServerMain
💬 Run the client (can open multiple windows):

bash
Copy
Edit
java ClientGUIMain
